# Core dependencies for Blender Movie Director
# These are the minimal requirements needed to run the addon

# Async HTTP client for service discovery
aiohttp>=3.9.0

# Configuration file support
pyyaml>=6.0

# NumPy - pinned for Blender compatibility
numpy<2.0

# Optional but recommended AI integration libraries
# Uncomment these if you want to use the AI features:
# crewai>=0.1.0
# litellm>=1.0.0
# gradio_client>=0.8.0